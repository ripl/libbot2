package bot_lcmgl;

struct data_t {
    string   name;           // Name of the image data
    int32_t  scene;          // Incremented for each buffer flip
    int32_t  sequence;       // Message number in this buffer

    // Raw data goes here
    int32_t  datalen;
    byte     data[datalen];
}
